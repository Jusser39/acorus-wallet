# AI Memory and Rules for Acorus Wallet

**CRITICAL INSTRUCTIONS FOR ALL AI AGENTS:**
1. **Log Every Action**: You must document significant decisions and actions.
2. **Push to GitHub**: ALL changes must be committed and pushed to GitHub (`origin main`). Do not leave changes uncommitted.
3. **Deploy Immediately**: Ensure that all changes are pushed to the `main` branch immediately so they can be deployed to the server (Vercel). Do not push to `master`. The production branch is `main`.

**Context Notes:**
- The application uses Next.js.
- The UI language must be in Russian where applicable, responding to the user in Russian.
- Production deployment is triggered by pushing to the `main` branch.
