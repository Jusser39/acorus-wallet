# AI Memory and Rules for Acorus Wallet

**CRITICAL INSTRUCTIONS FOR ALL AI AGENTS:**
1. **Log Every Action**: You must document significant decisions and actions in this `.ai_memory.md` file.
2. **Push to GitHub**: ALL changes must be committed and pushed to GitHub (`origin main`). Do not leave changes uncommitted.
3. **Deploy Immediately (VPS)**: The server does NOT auto-deploy from GitHub. After making any changes, you MUST:
   - Run the latest deploy script: `python scripts/deploy_wave6.py` (use the `run_command` tool in background and wait for it to finish).
   - This script creates a tarball, uploads it to the VPS at `85.239.59.199`, and runs the docker build process. You MUST wait for it to finish to verify the deployment.
4. **No Unfinished Work**: After editing code, always deploy to VPS and push to GitHub within the same session.

**Context Notes:**
- The application uses Next.js.
- The UI language must be in Russian where applicable, responding to the user in Russian.
- Production deployment is **NOT** automatic; it requires executing the python deployment script explicitly.
